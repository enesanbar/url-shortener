{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run (Prod)",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/rest/main.go",
            "cwd": "${workspaceFolder}",
            "env": {
                "DEPLOY_TYPE": "prod",
                "ENV_PREFIX": "NC",
            },
            "buildFlags": [
                "-race"
            ]
        }
    ]
}